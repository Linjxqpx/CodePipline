version: 0.2

phases:
   install:
        runtime-versions:
            dotnet: 6.0

   pre_build:
        commands:
            - pwd
            - cd /DeployLambda
            - pwd

   build:
        commands:
            - dotnet restore
            - dotnet publish -p:PublishReadyToRun=false -c Release -o ./output
   artifacts:
        files:
            - /DeployLambda/output

